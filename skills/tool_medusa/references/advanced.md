# Advanced Techniques

## Modules & Inputs
- Use `-M <module>` and `-q` to view module usage.
- Use `-C` combo files or separate `-U/-P` lists for inputs.

## Throttling & Resume
- Control concurrency with `-t` (logins) and `-T` (hosts).
- Use `-Z` resume maps to continue interrupted scans.
