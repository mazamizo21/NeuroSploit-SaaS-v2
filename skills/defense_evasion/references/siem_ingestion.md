# SIEM Ingestion Checks

## Goals
- Confirm logs are forwarded to SIEM or central logging.
- Identify sources not ingested or missing retention.

## Evidence
- Summarize ingestion status in `logging_coverage.json`.
- Document gaps in `findings.json`.
