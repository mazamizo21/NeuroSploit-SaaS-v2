# Advanced Techniques

## Rule Design
- Use `nocase`, `wide`, and `ascii` string modifiers to tune matching across encodings.
- Use `of` and `them` quantifiers to require multiple strings before a match.

## Evidence
- Store the exact rule file and command invocation used for each scan.
