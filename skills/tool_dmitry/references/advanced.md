# Advanced Techniques

## Flag Selection
- Use `-w`/`-i` for whois, `-s` for subdomains, `-e` for emails, `-p` for ports.
- Use `-t` to tune TTL/timeout for port scans.

## Output
- Use `-o` to save results to a file per target.
