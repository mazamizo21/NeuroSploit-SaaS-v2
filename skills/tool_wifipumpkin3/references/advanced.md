# Advanced Techniques

## Requirements & Ports
- Requires a Wi‑Fi adapter that supports AP mode.
- Uses port 53 for its DNS server; ensure the port is free before start.

## CLI Control
- `--rest` enables the REST API and `--restport` sets the port.
- `-s` sets a session name for continuing an attack.
