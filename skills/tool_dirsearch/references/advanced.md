# Advanced Techniques

## Recursion
- Use `--recursive` only when explicitly authorized.
- Keep recursion depth low to avoid runaway scans.

## Extensions
- Set `-e` to only the relevant file types for the stack.
