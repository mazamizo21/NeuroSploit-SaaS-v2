# Advanced Techniques

## Targeting
- Use `-M` to select MITM mode and specify explicit target pairs.
- Use `-f` to apply a pcap filter string and reduce captured traffic.

## SSL MITM
- Control SSL MITM behavior explicitly and avoid forging certificates unless authorized.
