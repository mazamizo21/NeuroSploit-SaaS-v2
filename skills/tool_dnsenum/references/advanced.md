# Advanced Techniques

## Threading & Timeouts
- Tune `--threads` and `--timeout` to avoid overwhelming DNS servers.
- Use `--noreverse` when reverse lookups are not needed.

## Brute Force
- Use `-f` for custom wordlists and `-r` for recursive subdomain checks only when approved.
