# Advanced Techniques

## Response Control
- Use `--fakedomains`/`--truedomains` and fake record types to scope spoofing.
- Use `--file` to map specific domains to custom IPs.

## Runtime Settings
- Configure `--nameservers`, `--interface`, and `--port` for safe proxying.
