# Advanced Techniques

## Traversal Controls
- Use `--traverse` to look for nearby IP space only within approved ranges.
- Use `--wide` sparingly; it expands to /24 adjacency.

## Validation
- `--connect` can validate HTTP connectivity on discovered hosts.
