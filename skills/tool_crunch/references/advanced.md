# Advanced Techniques

## Charset Order
- Character set order must be: lowercase, uppercase, numbers, symbols.

## Pattern Mode
- Use `-t` to define patterns with placeholders (`@`, `,`, `%`, `^`).
- Use `-b` or `-c` to split large outputs into manageable files.
