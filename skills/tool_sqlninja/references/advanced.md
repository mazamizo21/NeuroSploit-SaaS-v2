# Advanced Techniques

## Config-driven Operation
- Use `sqlninja.conf` to define target URL, injectable parameters, and auth.
- Use `-m` modes (test, fingerprint, bruteforce) in a controlled sequence.
