# Advanced Techniques

## Interface & Scope
- Use `--interface` to target the correct NIC when multiple interfaces exist.
- Use `--localnet` to scan the directly connected subnet or specify ranges explicitly.

## Output
- Save scan outputs for evidence and correlation with other L2 tools.
