# Advanced Techniques

## Engine Selection
- Use `-e` to select a subset of engines for stability or scope control.
- Use `-b` to enable bruteforce only when authorized and timeboxed.

## Port Validation
- Use `-p` for limited port checks on discovered subdomains.
