# Advanced Techniques

## Rate Control
- Use `--rate` to avoid overwhelming targets; keep rates conservative.
- Store scan settings in a config file with `-c` for repeatability.

## Banner Checks
- Masscan can perform basic banner checks for select protocols; use only when authorized.
