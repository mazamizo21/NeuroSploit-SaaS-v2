# Advanced Techniques

## Rules Syntax
- Wordlist rules allow transforms (case, append, prepend, rotate) to expand candidates.
- Use simple commands and preprocessor ranges to generate multiple rule variants.
