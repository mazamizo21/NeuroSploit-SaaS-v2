# Advanced Techniques

## Offline Dictionary Mode
- Use `-r` with a capture file and `-f` with a wordlist; supply SSID with `-s`.
- Use `-c` to validate 4‑way handshakes without cracking.

## Precomputation
- Use `genpmk -f <wordlist> -d <output> -s <SSID>` to precompute hash files.
