# Ghidra Workflow

## Steps
1. Create a new project and import the binary.
2. Run auto-analysis with default options.
3. Label functions of interest and extract indicators.

## Output
- Record interesting functions and strings in `findings.json`.
- Capture evidence screenshots or notes in `evidence.json`.
