# Advanced Techniques

## Modes
- Use `dir`, `dns`, or `vhost` modes based on the target.
- Limit extensions with `-x` and keep concurrency conservative to reduce noise.

## Output Control
- Use output files to preserve findings and support reproducibility.
