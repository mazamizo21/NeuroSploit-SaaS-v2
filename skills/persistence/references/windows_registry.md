# Windows Registry Persistence

## Goals
- Review Run/RunOnce keys and related autorun locations.
- Capture registry paths and associated executables.

## Evidence
- Summarize registry entries in `persistence_inventory.json`.
- Document risky entries in `persistence_risks.json`.
