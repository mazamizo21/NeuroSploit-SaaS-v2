# Linux Shell Profile Persistence

## Goals
- Identify profile scripts that execute on login or shell start.
- Record file paths and suspicious entries.

## Evidence
- Summarize profile hooks in `persistence_inventory.json`.
- Document risky hooks in `persistence_risks.json`.
