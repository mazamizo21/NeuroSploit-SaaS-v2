# S3 Public Access Block Review

## Goals
- Confirm account-level and bucket-level public access block settings.
- Identify buckets lacking public access blocks.

## Notes
- Prefer read-only queries and inventory outputs.

## Evidence
- Record status in `cloud_storage.json`.
- Document risky buckets in `findings.json`.
