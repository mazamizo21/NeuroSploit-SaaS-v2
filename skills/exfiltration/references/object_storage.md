# Object Storage Sources

## Goals
- Identify object storage buckets and public exposure.
- Validate access with metadata only.

## Evidence
- Summarize object storage exposure in `data_sources.json`.
- Document risky buckets in `findings.json`.
