# Database Sources

## Goals
- Identify database assets holding sensitive data.
- Validate access with metadata only.

## Evidence
- Summarize database sources in `data_sources.json`.
- Document sensitive tables or schemas in `findings.json`.
