# Firewall Posture

## Goals
- Identify overly permissive firewall rules.
- Flag public ingress with wide source ranges.

## Evidence
- Summarize firewall posture in `cloud_network.json`.
- Document risky rules in `findings.json`.
