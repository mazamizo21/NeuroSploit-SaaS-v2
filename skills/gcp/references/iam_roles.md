# IAM Role Review

## Goals
- Identify broad roles (Owner, Editor) and custom roles with risky permissions.
- Map role bindings to projects and service accounts.

## Evidence
- Summarize risk roles in `cloud_iam.json` and `findings.json`.
