# Advanced Techniques

## Workspaces
- Use `workspaces create/load/list/remove` to isolate data per engagement.
- Workspaces persist configuration and data between sessions.

## Marketplace & Keys
- Use `marketplace` commands to install modules and note dependency/key requirements.
- Configure API keys before running modules that depend on external services.
