# Advanced Techniques

## Methods
- `VRFY`, `EXPN`, and `RCPT` are supported; `RCPT` is often most reliable.
- Use `-D` to append a domain when enumerating email addresses.

## Performance
- Use `-m` to control process count and `-w` for response timeouts.
