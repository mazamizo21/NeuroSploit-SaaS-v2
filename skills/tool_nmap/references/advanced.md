# Advanced Techniques

## NSE Script Control
- Use `-sC` for default scripts or `--script` to select categories/files.
- Use `--script-args` for script-specific parameters and keep credentials scoped.

## Script Safety
- Be aware that some NSE categories can be intrusive; limit to safe categories unless authorized.
